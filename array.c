#include<cs50.h>
#include<stdio.h>

int main (void)
{
    int lengh;
    do
    {
        lengh = get_int("lengh: ");
    }
    while(lengh < 1);

    int lengh[];
    for(int i = 1; i < lengh, i++)
    {
        printf("%i\n", lengh[i] *2)
    }
}