#include<cs50.h>
#include<stdio.h>
#include<string.h>

int main (void)
{
    string word = get_string("word: ");

    int word_length = strlen(word);

    for(int i = 0; i < word; i++)

    prog[i] = word[0] < word[1];
    printf("%s\n", prog[i])
}