def main():
    # empty list
    List = []
    # ask input
    # add item to list
    # keep count of items
    try:
        while True:
            groceries = input("")
            if groceries
            count_groceries = int(groceries)

    except EOFError:
             pass

def list(li):
    total = sum(li.values())
    print(f"{groceries.count()} {total}")

main()
